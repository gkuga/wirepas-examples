[project]
name = "hello"
version = "0.1.0"
description = "Wirepas Gateway-to-Backend API hello example, no hardware required"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paho-mqtt>=2.1.0",
    "wirepas-mesh-messaging>=1.4.1",
]
